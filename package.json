{
  "name": "eh-orbiting-circles",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "live-server --port=3000 --entry-file=index.html"
  },
  "devDependencies": {
    "vite": "^4.4.5",
    "live-server": "^1.2.2"
  }
}
